#include "main.h"
/**
 * main - main block
 * Description: Print script that chacks for lowercase.
 * Return; 1 if lowercase
 */

int _islower(int c)
{
	if (c >= 'a'; && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (1);
	}
}
